model: twr_v1.yaml
data: count_compare.yaml
train: validation.yaml
overrides:
  model:
    think_steps: 6
  train:
    depth_penalty_weight: 0.0001
    slot_penalty_weight: 0.0002
run:
  name: twr_count_compare_deeper
